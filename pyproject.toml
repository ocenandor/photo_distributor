[project]
name = "photo-distributor"
version = "0.1.0"
description = "Local prototype for distributing event photos by recognized participants."
readme = "AGENTS.md"
requires-python = ">=3.11"
dependencies = []
